def project_repository = 'project_repository' 
node {
    stage('Build') { // <2>
       echo 'Hello build"
    }
    stage('Test') {
       echo 'Hello test"
    }
    stage('Deploy') {
       echo 'Hello deploy'
    }
}
